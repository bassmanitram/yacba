name: Code Quality

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
  pull_request:
    branches:
      - main
      - develop
      - 'feature/**'

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run pre-commit hooks
      run: |
        pip install pre-commit
        pre-commit run --all-files || true

    - name: Security check with bandit
      run: |
        pip install bandit
        bandit -r strands_agent_factory/ -f json -o bandit-report.json || true
        bandit -r strands_agent_factory/ -f txt || true

    - name: Upload bandit report
      if: always()
      uses: actions/upload-artifact@v5
      with:
        name: bandit-report
        path: bandit-report.json
        if-no-files-found: warn

    - name: Check package build
      run: |
        pip install build
        python -m build
        pip install twine
        twine check dist/*

    - name: Test package installation
      run: |
        pip install dist/*.whl
        python -c "import strands_agent_factory; print('Package installed successfully')"
